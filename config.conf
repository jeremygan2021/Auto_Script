# 部署配置文件 - 由配置向导自动生成
# 生成时间: Sun Jun 22 17:22:03 CST 2025

# ==================== 本地配置 ====================
# 本地项目构建目录
BUILD_DIR="/Users/jeremygan/Desktop/TangledupAI"
# 构建命令
BUILD_COMMAND="npm run build"
# 包管理器
PACKAGE_MANAGER="npm"

# ==================== 服务器配置 ====================
# 服务器连接信息
SERVER="6.6.6.8"
PASSWORD="123"

# 服务器路径配置
REMOTE_PROJECT_DIR="~/"
REMOTE_SCRIPT_DIR="/root"

# 服务配置
SERVICE_PORT=3000
SERVICE_NAME="my-app"

# 部署类型配置
DEPLOY_TYPE="auto"

# 静态文件服务配置
STATIC_SERVER_COMMAND="nginx"
STATIC_SERVER_LOG="/var/log/${SERVICE_NAME}-server.log"

# ==================== Nginx配置 ====================
# 是否启用Nginx反向代理
ENABLE_NGINX="false"
# 域名配置
DOMAIN_NAME=""
# Nginx配置文件路径
NGINX_CONFIG_PATH="/etc/nginx/sites-available"
# Nginx启用配置路径  
NGINX_ENABLED_PATH="/etc/nginx/sites-enabled"
# 内部应用端口（Nginx代理的目标端口）
INTERNAL_PORT="3000"
# Nginx监听端口
NGINX_PORT="80"

# ==================== 文件配置 ====================
ZIP_FILE="build.zip"

# ==================== 健康检查配置 ====================
# 目标URL用于健康检查
TARGET_URL=""
# 健康检查关键词
HEALTH_CHECK_KEYWORD=""
# 等待服务启动时间（秒）
STARTUP_WAIT_TIME=5

# ==================== 监控配置 ====================
# 监控检查间隔（秒）
MONITOR_INTERVAL=30
# 重启尝试次数
MAX_RESTART_ATTEMPTS=3
# 日志文件路径
LOG_FILE="/var/log/app-monitor.log"
